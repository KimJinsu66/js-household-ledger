<%= form_with(model: user_household_ledger) do |form| %>
  <% if user_household_ledger.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_household_ledger.errors.count, "error") %> prohibited this user_household_ledger from being saved:</h2>

      <ul>
        <% user_household_ledger.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :balance %>
    <%= form.number_field :balance %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
