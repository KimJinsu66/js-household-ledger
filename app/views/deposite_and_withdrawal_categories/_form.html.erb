<%= form_with(model: deposite_and_withdrawal_category) do |form| %>
  <% if deposite_and_withdrawal_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(deposite_and_withdrawal_category.errors.count, "error") %> prohibited this deposite_and_withdrawal_category from being saved:</h2>

      <ul>
        <% deposite_and_withdrawal_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
